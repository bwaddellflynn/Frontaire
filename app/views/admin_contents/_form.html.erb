<%= form_with(model: admin_content) do |form| %>
  <% if admin_content.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_content.errors.count, "error") %> prohibited this admin_content from being saved:</h2>

      <ul>
        <% admin_content.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :header, style: "display: block" %>
    <%= form.text_field :header %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
